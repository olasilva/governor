# TODO - Fix landing page (home vs index)

- [ ] Confirm which page must be served at site root (`/`).
- [ ] If you want `home.html` to be the root page: rename `home.html` -> `index.html` (and rename existing `index.html` to `dashboard.html` or similar).
- [ ] If renaming is not possible: update/replace `index.html` to redirect to `home.html`.
- [ ] Re-deploy and verify:
  - [ ] Visiting `/` shows the home/landing content.
  - [ ] Navigation links still work (especially dashboard links).

